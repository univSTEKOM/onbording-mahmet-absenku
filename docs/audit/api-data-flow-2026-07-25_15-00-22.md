# API Data Flow Audit

**Date:** 2026-07-25 15:00:22
**Scope:** Frontend ? Mock-API data flow, type contracts, response integrity

---

## ?? CRITICAL (2)

### 1. mainCategory & subCategory required di tipe Absensi tapi TIDAK ADA di data db.json lama

| Aspek | Detail |
|-------|--------|
| **File** | 	ypes/index.ts:49-62 vs db.json absensi entries |
| **Dampak** | Akses bsensi.mainCategory ? undefined ? bisa crash runtime |
| **Penyebab** | Field ditambahkan ke tipe setelah data seed dibuat. 
ormalizeDbFile() tidak menambahkannya. |
| **Fix** | Jadikan mainCategory?: string, subCategory?: string di tipe, ATAU migrasi data via 
ormalizeDbFile() |
| **Risiko Fix** | Opsional = aman. Migrasi data = sedang (perlu update db.json). |

### 2. password: string (required) di tipe User tapi TIDAK PERNAH dikembalikan API

| Aspek | Detail |
|-------|--------|
| **File** | 	ypes/index.ts:33-47 vs semua endpoint user |
| **Dampak** | Pelanggaran kontrak type. mergeUserData hardcode '' (useAuth.ts:16) |
| **Fix** | Jadikan password?: string opsional |
| **Risiko Fix** | Aman — tidak ada kode yang mengakses user.password |

---

## ?? MEDIUM (3)

### 3. checkIn kirim field yang ditimpa backend

| Aspek | Detail |
|-------|--------|
| **File** | pi/absensi.ts:40-51 vs server.js:418-448 |
| **Detail** | Frontend kirim status: 'hadir', mainCategory, subCategory tapi backend middleware **replace total** body-nya (line 427-437) |
| **Fix** | Hapus field redundant dari body checkIn, biarkan backend yang isi |
| **Risiko Fix** | Aman — nilai yang dikirim tidak pernah dipakai |

### 4. UpdateUserData punya ole?: Role tapi self-service PATCH hapus ole

| Aspek | Detail |
|-------|--------|
| **File** | 	ypes/index.ts:76-85 vs server.js:485 |
| **Detail** | delete body.role di PATCH /users/:id (self-service). Admin endpoint /api/users/:id menerima ole tapi frontend tidak pernah panggil |
| **Fix** | Hapus ole dari UpdateUserData (hanya admin yang bisa ubah role) |
| **Risiko Fix** | Aman — frontend tidak pernah kirim ole via self-service |

### 5. Data face descriptor masih di oto untuk 3 user

| Aspek | Detail |
|-------|--------|
| **File** | db.json — users udi, susmanto, llfk |
| **Detail** | oto berisi JSON array face descriptor, bukan base64 image. aceDescriptor field kosong/tidak ada |
| **Fix** | Pindahkan data face descriptor ke field aceDescriptor, reset oto ke '' |
| **Risiko Fix** | Sedang — user perlu registrasi ulang face recognition |

---

## ?? LOW (2)

### 6. Extra field di response dashboard

| Aspek | Detail |
|-------|--------|
| **File** | pi/dashboard.ts:10-30 vs server.js |
| **Detail** | AdminWeekChartItem tidak punya present, bsentPermit, bsentUnpermit (ada di response). AdminWeekSummary tidak punya presentMonth, permitMonth, unpermitMonth |
| **Fix** | Tambah field ke tipe atau abaikan (tidak ngefek runtime) |

### 7. ApiResponse<T> tidak pernah dipakai

| Aspek | Detail |
|-------|--------|
| **File** | 	ypes/api.ts:1-4 |
| **Detail** | Didefinisikan sebagai generic wrapper tapi tidak di-import di API service mana pun |
| **Fix** | Hapus atau implementasikan |

---

## ? OK (Tidak Ada Masalah)

| Data Flow | Status |
|-----------|--------|
| GET /pengajuan ? Pengajuan[] | ? Full match |
| POST /pengajuan ? createPengajuan | ? Full match |
| PATCH /pengajuan/:id ? updateStatus | ? Full match |
| DELETE /pengajuan/:id | ? Full match |
| GET /api/dashboard/recent ? RecentAbsensiItem[] | ? Full match |
| GET /api/dashboard/month ? MonthAttendanceData | ? Full match |
| GET /api/attendance-categories | ? Full match |
| GET /api/absensi/search | ? Full match |
| GET /api/me | ? Full match |
| POST /api/register | ? Full match |
| PATCH /absensi/:id (checkOut) | ? Full match |
| Semua filter types | ? Didukung backend |
| Better Auth session | ? Full match |

---

## Prioritas Eksekusi

| Priority | Task | Effort | Risk |
|----------|------|--------|------|
| **P0** | 1a. mainCategory/subCategory jadi opsional di tipe | 5 menit | ?? Rendah |
| **P0** | 2. password jadi opsional di tipe User | 2 menit | ?? Rendah |
| **P1** | 3. Hapus field redundant dari body checkIn | 10 menit | ?? Rendah |
| **P1** | 4. Hapus ole dari UpdateUserData | 5 menit | ?? Rendah |
| **P2** | 5. Pindah data face descriptor 3 user | 15 menit | ?? Sedang |
| **P3** | 6. Tambah extra field di tipe dashboard | 10 menit | ?? Rendah |
| **P3** | 7. Hapus ApiResponse<T> | 2 menit | ?? Rendah |
